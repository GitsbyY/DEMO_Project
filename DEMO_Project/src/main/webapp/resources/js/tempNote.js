

function goPage(pageNumber) {
		var curPageObj = $("#curPage")
		
		curPageObj.val(pageNumber);
		
		alert(curPageObj.val());
		
		
		var pagingFormObj = $('#pagingForm');
		pagingFormObj.submit(); 
		
	}
	
	var numFaceObj = document.getElementsByName('numFace');
	numFaceObj.setAttribute('style','background-color : red;');
	document.getElementsByClassName
	var numClass = document.getElementsByClassName('numClass');
	
	